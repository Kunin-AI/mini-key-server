on:
  workflow_dispatch:

name: Build and upload container

jobs:
  build-backend:
    name: Build backend
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIAYKQBGMILJ4KWMRP7
        aws-secret-access-key: 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 # todo cycle these creds after I have access to secrets
        aws-region: ca-central-1

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Build and push
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: ${{ steps.login-ecr.outputs.registry }}/mini-key-server:dev-${{ github.sha }}, ${{ steps.login-ecr.outputs.registry }}/mini-key-server:latest
        cache-from: type=gha
        cache-to: type=gha
